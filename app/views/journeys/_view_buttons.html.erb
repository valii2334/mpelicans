<% if can?(:show, journey) %>
  <div class="row mb-3">
    <div class="col-12">
      <%= link_to 'View Journey', journey_path(journey), class: 'btn btn-primary float-end mt-n1' %>
    </div>
  </div>
<% end %>